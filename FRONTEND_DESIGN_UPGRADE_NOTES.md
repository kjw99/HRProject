# Frontend Design Upgrade Notes

## HR 공통

- `HrPageHero`의 시각 밀도가 높은 편이라, 모바일에서는 `stats` 3개 이상일 때 1열 스택으로 강제하는 반응형 규칙을 추가하면 읽기가 더 편해집니다.
- 현재 `HrModal` 계열은 기능적으로는 안정적이지만 모달마다 액션 영역 높이와 여백이 조금씩 달라 보입니다. footer 버튼 높이, 간격, 위험 버튼 색상 계층을 공통 토큰으로 맞추면 완성도가 올라갑니다.
- `HrSidebar`는 메뉴 수가 늘어난 상태라서 섹션 구분선 또는 그룹 제목이 있으면 정보 구조가 더 선명해집니다.

## 지원자 관리

- `ApplicantDetailModal`은 정보 밀도가 높아졌으므로, 왼쪽 정보 영역 안에서 `기본 정보 / 예약 상태 / 우대 조건`을 카드 섹션으로 더 명확히 나누면 스캔 속도가 좋아집니다.
- 초대 이력 카드에 `유효`, `회수`, `만료`를 텍스트만이 아니라 아이콘과 배경색으로 더 분명히 구분하면 상태 인지가 빨라집니다.
- `ApplicantListClient`의 액션 버튼이 많아졌으므로 데스크톱에서는 `상세`만 남기고 나머지를 상세 모달 내부로 더 강하게 유도하는 것도 좋은 정리 방법입니다.

## 면접관 커뮤니케이션

- `InterviewerCommunicationClient`는 운영 도구 성격이 강하므로 좌측 리스트와 우측 가이드 카드 사이 대비를 조금 더 키우면 집중도가 좋아집니다.
- `InterviewerInviteModal`, `InterviewerMailComposerModal` 모두 결과 영역이 생성 후 아래쪽에 나타나는데, 성공 결과를 상단 고정 배너처럼 보여주면 다음 행동이 더 분명해집니다.
- 메일 작성 모달은 템플릿 변수 프리뷰 패널이 추가되면 실수 방지에 도움이 됩니다.

## Admin

- `AdminHomeOverview`는 역할 분리 목적은 잘 드러나지만, Admin 전용 카드와 HR 연결 카드의 스타일 차이를 조금 더 키우면 경계가 더 분명해집니다.
- 관리자 홈 상단에 `최근 변경`, `사용자 수`, `비밀번호 재설정` 같은 운영 지표 카드가 추가되면 진짜 홈 화면 느낌이 더 살아납니다.

## Interviewer 초대 수락 페이지

- `InterviewerInviteAcceptClient`는 현재 단일 CTA 중심 구조라 충분히 명확하지만, 성공 이후 `다음 단계`를 카드 2~3개로 보여주면 사용자가 이동 후 무엇을 해야 하는지 더 자연스럽게 이해할 수 있습니다.
- 초대 만료/실패 상태에서 `HR에 문의하기`, `다시 링크 요청` 같은 대안 액션이 보이면 운영 측면에서 더 친절한 UX가 됩니다.
